/** Default page size for paginated queries (leads, campaigns, etc.) */
export const PAGE_SIZE = 20

/** Maximum leads per scoring request */
export const MAX_LEADS_PER_SCORING_REQUEST = 100

/** Concurrency limit for scoring pipeline */
export const SCORING_CONCURRENCY_LIMIT = 5

/** Maximum rows allowed in a single CSV export */
export const MAX_EXPORT_ROWS = 100_000
